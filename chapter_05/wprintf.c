#include <stdio.h>
#include <wchar.h>

int main(void)
{
	int weekday = 3, month = 10, day = 27, year = 1970,
	    hour = 22, min = 30
	wprintf(L"%s, %d %s %d, %d:%.2d\n",
		weekday, year, );

	return 0;
}
